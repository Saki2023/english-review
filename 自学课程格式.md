# 出门完整自学课程格式

学习窗口在用户出门前生成 `学习同步/网站自学课程.json`。完整同步脚本发现该文件后，会使用教学写入令牌把课程幂等上传给 `.sync.env` 中的指定账号；令牌、密码和 Cookie 不得写入课程文件。

## 顶层结构

```json
{
  "updatedAt": "2026-08-07T12:00:00+08:00",
  "lessons": [],
  "removeLessonIds": []
}
```

- `lessons` 至少包含一天，建议出门前一次放入 2～7 天。
- `lessonId`、`stageId`、`stepId` 和待学习词句的 `id` 必须长期稳定。重复上传同一 ID 会更新未开始版本，不会重复创建。
- `removeLessonIds` 可删除尚未开始的课程；已经开始的课程继续使用开始时保存的不可变快照。
- 上传课程不会自动开启出门模式，也不会把未来词句写入正式词句库。

## 每天课程

```json
{
  "lessonId": "trip-day-009",
  "studyDay": 9,
  "formalDate": "2026-08-08",
  "title": "第 9 天完整自学课程",
  "version": "1",
  "enabledFrom": "2026-08-08T00:00:00+08:00",
  "expiresAt": "2026-08-20T00:00:00+08:00",
  "plannedContent": {
    "words": [],
    "supplements": [],
    "sentences": [],
    "note": {}
  },
  "stages": [],
  "nextPreview": "下一课预习说明"
}
```

`plannedContent.words` 使用现有正式单词字段：`id`、`english`、`chinese`、`phonetic`、`pronunciation`、`acceptedChinese`、`directions`、`example`、`exampleZh`。`plannedContent.sentences` 使用 `id`、`english`、`chinese`、`acceptedChinese`、`acceptedEnglish`、`directions`。服务器会强制把它们保存为 `planned`，忽略上传方伪造的 `learned` 状态。

词形、相关发音变化和紧邻语法放入独立 `plannedContent.supplements`，不能塞进 `words` 占新基本词名额。每个补充组必须有稳定 `id`、`type`、`title`、`note` 和 `forms`；每个 form 必须显式登记稳定 `id`、表面词形 `english`、基本词 `wordId`、基本形 `lemma`、`phonetic`、`pronunciation` 与 `grammarTags`。服务器会验证 `wordId + lemma` 是否对应本课基本词或正式已学基本词，绝不通过删除 `s/es` 猜测。例如：

```json
{
  "id": "d15-supp-third-person-s",
  "type": "word-forms",
  "title": "动作动词第三人称单数",
  "note": "只作词形和紧邻语法练习，不占新词名额。",
  "forms": [
    {
      "id": "d15-form-has",
      "english": "has",
      "wordId": "d15-have",
      "lemma": "have",
      "phonetic": "/hæz/",
      "pronunciation": "词尾 s 接近 /z/。",
      "acceptedChinese": ["有"],
      "grammarTags": ["third-person-singular"]
    }
  ]
}
```

补充 form 可在预习、自学和句子题中出现；真实 usage 始终写到 `wordId` 对应的基本词，并可在同一事件保存 `formId/surfaceForm` 审计字段。form 不转成词库行、不创建独立 task/SRS/memory 卡、不增加词汇量或覆盖额度。

课程未完成时，这些内容不会出现在普通复习、错题、能力证据、已学数量或正式词句库。六阶段全部完成后，服务器在一个可恢复事务中完成以下动作：

1. 把词、句和结构化笔记转为正式内容。
2. 记录 `learnedAt`、`sourceLessonId`、`firstReviewDue`。
3. 若同一单词此前是预习词，移除预习记录并只保留一份正式词。
4. 不重置已有复习等级，不重复创建词句或证据。
5. 最后才把当天课程标记为 `completed`；服务重启会先恢复未完成事务。

## 固定六阶段

`stages` 必须恰好按以下顺序提供，每阶段至少一个步骤：

| 顺序 | `type` | 建议 `stageId` | 内容 |
|---|---|---|---|
| 1 | `review` | `review` | 旧知识复习 |
| 2 | `phonics` | `phonics` | 拼读与词汇 |
| 3 | `pattern` | `pattern` | 句子结构 |
| 4 | `reading` | `reading` | 阅读与翻译 |
| 5 | `test` | `test` | 10 题测验和原题订正 |
| 6 | `summary` | `summary` | 中文总结和下一课预习 |

第五阶段必须有 10 道正式题，并使用 `category` 明确分布：`phonics` 2 题、`en-zh` 2 题、`zh-en` 2 题、`reading` 4 题。首次得分只看每题第一次判定；之后订正单独保存，不能覆盖首次 9/10 一类成绩。

## 步骤字段

支持的 `type`：

- `teach`：讲解后点击“我已理解”；不计正确率。
- `read-aloud`：显示音标、中文发音辅助和喇叭，朗读确认不计正确率。
- `choice`：单选；`choices` 至少两个，答案写入 `acceptedAnswers`。
- `short-answer`：短回答。
- `en-zh`：英译中。
- `zh-en`：中译英。
- `reading-question`：阅读问题。
- `correction`：明确的订正步骤。
- `summary`：中文总结；不计正确率，但必填后才能完成。

通用示例：

```json
{
  "stepId": "test-en-zh-01",
  "type": "en-zh",
  "category": "en-zh",
  "instruction": "把英文译成中文。",
  "prompt": "It is a dog.",
  "english": "It is a dog.",
  "direction": "en-zh",
  "acceptedAnswers": ["它是一只狗", "它是只狗"],
  "correctionHint": "中文答案有一处关键信息不完整，请检查主语、对象或数量。",
  "contentId": "d9-dog-sentence",
  "gradingMode": "local",
  "formalEvidence": true
}
```

未作答前，服务器只向浏览器发送当前步骤的可见字段，不发送 `acceptedAnswers`、参考答案或后续步骤。英译中不会提前发送隐藏中文， 中译英不会提前发送隐藏英文。`correctionHint` 不能等于完整答案；应只指出主语、冠词、介词、be 动词、拼写或信息完整性等错误类型。

`gradingMode` 默认 `local`，复用现有同义答案、中文量词、普通 `deep` 的自然中文“很深/很深的”和英文功能词规则；`very/more/too/not deep` 等真实程度、比较或否定差异不会被放宽。只有确实需要语义判断的题才设置为 `ai`。AI 失败时答案保存为 `pending`，既不计错也不进入能力证据；用同一 `attemptId` 重试后再完成判定。

## 词汇边界

服务器会检查课程英文：只允许正式已学词、较早自学日已经明确计划的基本词与补充 form、本日 `plannedContent.words` 新基本词、本日显式登记的补充 form，以及当前题目可见原文中的词。未登记的词形仍会拒绝；隐藏参考答案和订正提示不得偷偷引入其他英文词。网页不会自行生成或扩展课程知识。

## 客户端和同步行为

- 所有提交使用稳定 `attemptId`；正确反馈后的“继续”使用稳定 `continueId`。响应丢失后重试不会重复计分。
- 当前步骤草稿同时保存在账号服务器状态和当前设备本地，显式暂停会立即保存暂停点。
- 刷新、重新登录、换设备或服务重启后恢复课程、版本、阶段、步骤、草稿、首次答案、全部订正和提问。
- `GET /api/sync/profile` 的 `selfStudyHistory` 导出逐日六阶段、首次答案、订正、待判、跳过、未作答、暂停点、有效时间和当前步骤提问；`selfStudyPlannedLessons` 单独列出尚未学习课程。
- 主动提问与讲解/朗读确认永远不是正确率证据。预习练习仍为 `formalEvidence: false`，不得与正式自学证据混合。

完整接口见 [API.md](API.md)。
