{
  "name": "daily-english-review",
  "version": "2.0.0",
  "private": true,
  "description": "Offline-first English review app with a Docker-ready API",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "user:add": "node server/create-user.js"
  },
  "engines": {
    "node": ">=20"
  }
}
