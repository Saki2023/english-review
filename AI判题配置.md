# 网页配置多套 AI 与 AI 出题

AI 的 Base URL、Key、模型、启用状态和调用方式全部在网页中配置，不需要通过 SSH 修改 `.env`。每套连接使用 OpenAI 兼容接口，可用于 sub2api、NewAPI、DeepSeek、OneAPI 及其他使用 Bearer Key 的常见 API 反代。

## 管理员配置

1. 登录英语复习软件的管理员账号。
2. 打开“AI 出题”。
3. 点击页面右上角的“AI 设置”。
4. 点击供应商列表中的加号添加一套连接，并填写名称、Base URL 和 API Key。
5. 点击“获取模型”，网站会读取当前供应商支持的模型。
6. 按需移除模型或添加自定义模型；其他供应商重复以上步骤。
7. 选择“手动固定”或“自动轮换”，再选择默认模型。
8. 点击“保存”，或用“保存并测试本套”测试当前正在编辑的供应商。

配置字段：

- `名称`：用于在设置和做题历史中区分供应商，例如 `sub2api`、`NewAPI` 或 `DeepSeek`。
- `启用此供应商`：停用后不参与实际调用或自动轮换，但配置仍会保留。
- `Base URL`：OpenAI 兼容地址，例如 `https://你的反代域名/v1`。可以包含 `/v1`、自定义前缀，也可以直接填写以 `/models`、`/chat/completions` 或 `/responses` 结尾的地址。
- `API Key`：当前供应商的 Key。已保存后输入框会清空，留空表示不修改原 Key。
- `可用模型`：从当前供应商的 `GET /models` 自动获取；兼容标准 OpenAI 列表及常见嵌套变体，也可以手动添加。
- `默认模型`：普通 AI 判题和首次进入 AI 出题时使用的模型。
- `超时`：上游请求最长等待时间，范围为 1 至 120 秒，默认 30 秒。推理强度较高时可适当增加。
- `每分钟上限`：每个账号每分钟最多发起的 AI 请求，范围为 1 至 60 次。

## 手动与自动轮换

- `手动固定`：只使用“当前供应商”。即使这套连接鉴权失败、限流、超时或上游故障，也不会自动调用其他供应商。模型选择只显示当前供应商的模型。
- `自动轮换`：使用所有已启用供应商的模型合集。每次调用从轮询位置开始，只尝试声明支持所选模型的供应商；当前供应商失败时继续尝试下一套，成功或失败后都会推进轮询位置。

设置首次创建以及旧单连接自动迁移后，调用方式都是`手动固定`。只有管理员主动切换为“自动轮换”并保存后，程序才会跨供应商切换。题组和做题历史会记录实际生成题目所用的供应商。

Key 只从浏览器通过 HTTPS 发送到英语软件服务器，保存于：

```text
/opt/english-review/server/data/ai-settings.json
```

该文件位于 Docker 持久化目录，权限为 `0600`，不会进入 GitHub，也不会在任何读取接口中返回 Key。自动更新备份会和其他账号数据一起备份它。

“获取模型”只读取当前供应商的模型列表，不会立即保存配置。点击“保存”或“保存并测试本套”后，多套供应商、调用方式和默认模型才会生效。

实际出题和判题会先尝试 OpenAI 兼容的 `POST /v1/chat/completions`。如果上游或所选模型不支持该接口，程序会自动改试 `POST /v1/responses`；Responses API 的思考强度会使用 `reasoning.effort` 字段发送。

模型出现在 `/models` 中只表示上游列出了它，不一定表示账号拥有调用权限。若上游返回无权限、额度不足、模型不兼容或超时，网页会显示对应的中文原因。

## 模型与强度

“AI 出题”页面可以为当前账号选择模型和强度：

- `轻度`（`low`）：优先速度，适合简单翻译和日常复习。
- `中`：速度与判断质量的平衡，默认选择。
- `高`：用于更难的句子，通常响应更慢。
- `极高`（`xhigh`）：更深入推理，耗时和用量通常明显增加。
- `最高`（`max`）：模型支持时使用最充分的推理，响应最慢。

强度使用 OpenAI 兼容的 `reasoning_effort` 参数。部分上游模型不支持该参数；遇到兼容错误时，服务器会自动移除强度参数后重试。

模型和强度选择按账号保存。普通复习中的 AI 判题也会使用该账号最后选择的模型和强度。

## 题目问答窗口

AI 生成题目后可以打开“问 AI”悬浮窗口。桌面端支持拖动、右下角缩放、最小化和最大化，手机端会自动限制在屏幕内。问答会关联当前题目并按账号保存，进入下一题时切换到新的题目上下文。

题目尚未提交时，AI 默认先给提示，不主动说出完整翻译或最终答案；明确询问某个单词或语法点时，只解释所问部分。问答不会提交题目，也不影响正确率。

## AI 出题依据

服务器会向所选模型提供以下学习数据：

- 已经学过的单词和句型
- 单词与句子的熟练等级
- 近期错题和正确答案
- 最近普通复习正确率
- 最近 AI 练习结果

生成的英文题目会经过服务器校验，只允许使用已经学过的英语词汇。题目顶部标签由服务器生成“介词辨析”“句型练习”等中性文字，不采用模型可能泄露答案的提示。未来日期的课程不会提前进入出题范围。

## 安全边界

- 只有管理员能查看和修改 AI 连接设置。
- 普通账号只能选择管理员允许的模型。
- 网页无法读取已保存的 Key。
- 生成题和 AI 练习历史按账号隔离。
- 不要把真实 API Key 发到聊天窗口或提交到 GitHub。
